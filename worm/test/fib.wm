"""#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
int v1_fib(int,int,int);
void main(){
printf("%d\\n", v1_fib(0, 1, 8));
printf("%d\\n", v1_fib(0, 1, 10));
}
int v1_fib(int a, int b, int n) {
while(n>0){
int v2_c = a
a = a + b
b = v2_c
n -= 1
}
return a;
}"""
@worm
def fib(a: int, b: int, n: int = 1) -> int:
    while n > 0:
        c = a
        a = a + b
        b = c
        n -= 1
    return a

@worm.entry
def main():
    printf("%d\n", fib(0, 1, 8))
    printf("%d\n", fib(0, 1, 10))

print(worm.dump_source())
